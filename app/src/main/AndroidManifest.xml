<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carpulin">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MisReservasActivity"></activity>
        <activity android:name=".Activities.MisViajesActivity" />
        <activity android:name=".Activities.ModificarPerfilPasajeroActivity" />
        <activity android:name=".Activities.PerfilPasajeroActivity" />
        <activity android:name=".Activities.ReservasSolicitadasActivity" />
        <activity android:name=".Activities.PerfilVehiculoActivity" />
        <activity android:name=".Activities.AgregarVehiculoActivity" />
        <activity android:name=".Activities.PerfilConductorActivity" />
        <activity android:name=".Activities.ModDatosConductorActivity" />
        <activity android:name=".Activities.ModVehiculoConductorActivity" />
        <activity android:name=".Activities.ModPasswordConductorActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Activities.InformacionViajeActivity" />
        <activity android:name=".Activities.ViajesEncontradosActivity" />
        <activity android:name=".Activities.BuscarViajeActivity" />
        <activity android:name=".Activities.RegistroActivity" />
        <activity android:name=".Activities.CrearViajeActivity" />
        <activity
            android:name=".Activities.PasajeroActivity"
            android:label="Carpulin Pasajero"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.ConductorActivity"
            android:label="@string/title_activity_conductor"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>